namespace AggregateConfig.Contracts
{
    public enum OutputTypeEnum
    {
        Json,
        Arm,
        Yml,
        Yaml = Yml
    }

    public enum InputTypeEnum
    {
        Json,
        Yml,
        Yaml = Yml
    }
}
